type SocialLink = {
  label: string;
  route: string;
  icon: string;
};

type NavLink = {
  label: string;
  route: string;
  icon?: string;
};

type DashboardNavLinks = {
  route: string;
  label: string;
  icon: string;
};
